# PostgreSQL 연결 정보
PGHOST=localhost
PGPORT=5432
PGDATABASE=lxc
PGUSER=postgres
PGPASSWORD=여기에_비밀번호_입력

# 선택 사항
PORT=8081
HOST=127.0.0.1

# Docker Engine socket. 비워 두면 Linux/macOS는 /var/run/docker.sock,
# Windows는 //./pipe/docker_engine 을 사용합니다.
DOCKER_SOCKET=
